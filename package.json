{
  "name": "apiserveresttest",
  "version": "1.0.0",
  "private": true,
  "description": "Testes automatizados de API (ServeRest) usando Playwright Test + GitHub Actions",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report",
    "ci": "playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "typescript": "^5.9.3"
  }
}
